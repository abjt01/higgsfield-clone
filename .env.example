# Neon — POOLED at runtime (host contains "-pooler"), UNPOOLED for migrations
DATABASE_URL=
DATABASE_URL_UNPOOLED=

# Google AI Studio, free tier, no card: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Vercel Blob
BLOB_READ_WRITE_TOKEN=

# openssl rand -base64 32
SESSION_SECRET=

# Optional. Force a provider for demoing the fallback: gemini | pollinations
IMAGE_PROVIDER=
